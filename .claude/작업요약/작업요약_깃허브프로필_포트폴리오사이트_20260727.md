# 작업요약 — GitHub 프로필 README·포트폴리오 사이트 (2026-07-27)

> `jsh340866/jsh340866` 프로필 레포의 README.md를 포트폴리오용으로 리뉴얼하고, `index.html` 포트폴리오 사이트를 신규 제작. GitHub 커밋 잔디를 뱀이 먹는 애니메이션도 추가.

## 변경 내용

### 1. README.md 리뉴얼
- 기존 빈 "풀스택 개발자" 소개를 걷어내고, valuepick 프로젝트(Spring Boot 백엔드, DART/공공데이터포털/한국수출입은행/KRX API 연동, 스케줄러·비동기 처리) 중심으로 재작성
- 자격증 섹션 추가: 정보처리기사(취득 완료), 빅데이터분석기사·SQLD(취득 예정)
- 소개 문구에서 "공무원 시험 준비" 등 진로 전환 배경 언급 제거, "데이터를 다루는 백엔드"라는 톤으로 정리 — 최종 목표는 데이터 분석가지만 현재 지원 직무·포트폴리오가 백엔드 중심이라 타이틀은 "Backend Developer" 유지하기로 결정
- 포트폴리오 사이트 링크 버튼 추가 (`https://jsh340866.github.io/jsh340866/` — Pages가 프로젝트 페이지로 서빙되는 주소, 레포명이 `jsh340866.github.io`가 아니라 `jsh340866`이라 루트 도메인이 아님)
- Contact 섹션: mailto 뱃지만으로는 기본 메일 프로그램이 없는 환경에서 클릭해도 반응이 없어서, 이메일 주소를 텍스트로도 병기
- 버그 수정: "Spring Boot" 뱃지 URL에 공백이 인코딩되지 않아 깨져 있던 것을 `%20`으로 수정

### 2. index.html — 포트폴리오 사이트 신규 제작
- `프로젝트작업/README.md`, `프로젝트작업/docs/ERD.md`, `.claude/작업요약/*`, `자소서-면접/*`, `임시/profile.md`를 모두 읽어서 실제 프로젝트 기여 내용·자기소개 스토리를 반영
- 다크모드 기본(라이트 토글 지원), Space Grotesk/JetBrains Mono 폰트, 그라디언트 메시 배경, 벤토 그리드, 스크롤 리빌 애니메이션으로 구성한 싱글 페이지
- 섹션 구성: Hero → About → Tech Stack → Certificates → ValuePick 프로젝트(담당 역할 4가지: 외부 API 파이프라인, 주가 수집 2700회→1회 최적화, 표준 계정 코드 매칭, 리츠/스팩 법령 기반 필터링) → 아키텍처 이미지 → 기능 데모(탭 클릭 시에만 해당 GIF 로드 — GIF 8개 총 70MB라 전체 사전로딩 방지) → 트러블슈팅 4건(문제/접근/결과 포맷) → 모바일 화면 → 팀 소개 → Contact
- 자소서·입사지원서 원본 docx는 개인정보라 링크하지 않고, 내용만 참고해 재구성
- About 섹션 문구는 사용자 피드백에 따라 여러 차례 수정 (토목공학 전공, 공무원 시험 준비 등 구체적 진로 전환 서사는 최종적으로 제거, "직접 호출하고 직접 확인합니다"라는 원칙 중심 톤으로 정리)

### 3. GitHub 프로필 Snake 애니메이션
- `.github/workflows/snake.yml` 신규 작성 — [Platane/snk](https://github.com/Platane/snk) 액션으로 커밋 잔디를 읽어 뱀이 먹는 SVG를 생성, `crazy-max/ghaction-github-pages`로 `output` 브랜치에 푸시
- 트리거: 매일 00:00 UTC 스케줄 + `main` push + 수동 실행(`workflow_dispatch`)
- 처음엔 `<picture>`로 라이트/다크 분기했으나, `prefers-color-scheme`가 GitHub 사이트 자체의 테마 토글이 아니라 브라우저/OS 설정을 따르는 특성 때문에 페이지는 라이트인데 뱀 이미지만 다크로 뜨는 미스매치 발생 → 분기 제거하고 이미지 하나로 고정하는 방향으로 전환
- 최종적으로 라이트 배경 + 블록 색상은 기여도 단계와 무관하게 회색(`#bdbdbd`) 한 톤으로 고정 — `outputs`에 `?color_dots=#ebedf0,#bdbdbd,#bdbdbd,#bdbdbd,#bdbdbd` 쿼리 파라미터 적용 (Platane/snk README 실제 확인 후 반영한 파라미터: `color_snake`, `color_dots`, `color_background`는 GIF 전용)

## 참고

- **GitHub Pages**: `index.html`이 실제로 `https://jsh340866.github.io/jsh340866/`에서 뜨려면 레포 Settings → Pages에서 Source를 `main` 브랜치(루트)로 켜야 함 — 이번 세션에서 확인/설정은 안 함
- **raw.githubusercontent.com 캐시**: 워크플로우가 새로 SVG를 생성해도 CDN이 이전 404/이전 이미지를 한동안 캐싱하는 경우가 있어, 워크플로우 완료 직후엔 README에서 이미지가 안 뜰 수 있음 (몇 분~최대 1시간 내 자동 해소, 실제로 겪음)
- 회색 고정 팔레트로 워크플로우를 바꾼 커밋(`7deaabb`)은 푸시 시점 기준 재실행 완료 여부 미확인 — 다음 확인 시 `output` 브랜치의 `github-contribution-grid-snake.svg` 색상이 회색으로 바뀌었는지 체크 필요
